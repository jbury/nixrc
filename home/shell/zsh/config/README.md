I already had these config files, so it just made more sense to keep using them.

config.zsh is the start point/is where the home-manager-generated .zshrc file actually points to pull all these configs in.
