Sorry in advance, ye who look at this.

Instead of using the home-manager zsh module's \*extra\* options, I just opted to set initContent to point at my confs directory by way of the `confs/config.zsh`, and just rely on my own zsh configuration skills over home-manager's options for the most part.
